[project]
name = "my-claude-squad"
version = "0.2.0"
description = "Collection of AI agent prompt templates, skills, and commands for use with Claude Code"
readme = "README.md"

# No code - just prompt template files
# See agents/, skills/, commands/ directories
